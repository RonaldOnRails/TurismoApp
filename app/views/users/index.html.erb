<h1>Listing pessoa_juridicas</h1>

<table id="tabelaData" class="display">
  <thead>
    <tr>
      <th>Código</th>
      <th>Email</th>
      <th>Admin</th>
      <th>Status</th>
    </tr>
  </thead>

  <tbody>
    <% @user.each do |user| %>
      <tr>
        <td><%= link_to user.id, edit_user_path(user.id) %></td>
        <td><%= user.email %></td>
        <td><%= user.is_admin %></td>
         <td><%= user.status %></td>
      </tr>
    <% end %>
  </tbody>
</table>

